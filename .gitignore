# Claude Code — local/personal settings (never committed; may hold secrets)
.claude/settings.local.json

# Claude Code — temporary / disposable working files
.claude/tmp/

# Sub-agent git worktrees (local working areas, never committed)
.claude/worktrees/

# --- Monorepo (steps 22-23) ---------------------------------------------------

# Dependencies
node_modules/

# Build output (per-package dist/, TS build info)
dist/
*.tsbuildinfo

# nx task-graph cache + generated graph dumps
.nx/
.nx-graph.json

# direnv local environment
.direnv/

