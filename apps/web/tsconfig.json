{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Editor / vue-tsc type-checking config for the Vue app. The app is BUILT by Vite (vite build), not tsc, so this project is intentionally absent from the root tsconfig.json solution. DOM + Vue SFC support is added on top of the strict workspace base.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "jsx": "preserve",
    "noEmit": true,
    "composite": false
  },
  "include": ["src/**/*.ts", "src/**/*.vue", "vite.config.ts", "vitest.config.ts"],
  "exclude": ["dist", "node_modules"]
}
